const config = {
            type: Phaser.AUTO,
            width: 720,
            height: 720,
            backgroundColor: 0xffffff,
            scene: [Chess]
        }

const game = new Phaser.Game(config);