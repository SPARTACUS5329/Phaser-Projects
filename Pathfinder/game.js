const config = {
            type: Phaser.AUTO,
            width: 500,
            height: 500,
            backgroundColor: 0x3399ff,
            scene: [Pathfinder]
        }

const game = new Phaser.Game(config);